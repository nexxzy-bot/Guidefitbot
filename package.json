{
  "name": "guidefit",
  "version": "2.4.0",
  "description": "GuideFit — Telegram Mini App и standalone-приложение: питание, вода, вес и тренировки",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "bot": "node bot.js",
    "check": "node --check server.js && node --check db.js && node --check bot.js && node --check static/sw.js && node --check scripts/backup-db.js && node scripts/check-frontend.js && node scripts/check-handlers.js",
    "test": "node --test",
    "backup": "node scripts/backup-db.js",
    "preheat": "node scripts/preheat-images.js",
    "generate": "node scripts/generate-dishes.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "sharp": "^0.35.4",
    "sqlite3": "^5.1.6"
  }
}
