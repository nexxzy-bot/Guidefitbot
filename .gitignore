node_modules/
.env
guidefit.db
guidefit.db-shm
guidefit.db-wal
guidefit.db.backup*
server.log
*.bak-*
static/*.bak*
nohup.out
*.log
sync.log
sync2.log
# резервные копии базы (npm run backup)
backups/

# Android: артефакты сборки и ключ подписи (не коммитить!)
android/build/
android/*.apk
android/*.keystore

# Локальный кеш фото (Pexels → webp), генерируется на сервере
static/images/cache/
# Локальные фото блюд (Unitools/Pexels → webp), генерируется scripts/preheat-images.js
static/images/dishes/

static/*.apk
android/*.apk

# стейт генератора блюд (runtime)
data/gen-state.json
